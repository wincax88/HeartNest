# HeartNest 视觉验收

- 验收日期：2026-09-23
- 参考：`docs/原型.png` 与同目录 `(2)` 至 `(6)` 的参考图
- 实现截图：`app/artifacts/visual-qa/*.png`（由 `tests/e2e/visual-smoke.mjs` 本地生成）
- 视口：390 × 844 CSS px，device scale factor 1
- 覆盖：引导、首页、陪伴者详情、聊天、回顾、个人中心、设置、会员

## 第一次验收

- P1：页面级 `uni-icons` 未被 npm 版 uni-ui 自动解析，品牌、底栏和操作图标出现空位。
- P2：聊天页头部的返回、头像和名称顺序不符合移动端阅读顺序。
- 其余：午夜蓝玻璃拟态、紫粉暖橙渐变、人物图裁切、中文层级、卡片间距和安全区表现与参考方向一致。

## 修正

- 增加项目内跨端 `uni-icons` 适配组件并通过 easycom 显式注册；8 个页面重新捕获后未发现未解析图标。
- 调整共享页头插槽顺序，聊天页按“返回 → 头像 → 名称与在线状态”排列。
- 增加真实 Chrome 的八屏截图脚本和完整主流程测试。

## 最终验收

- 8/8 页面在 390 × 844 视口完整渲染。
- 引导 → 首页 → Mika 详情 → 聊天回复 → 回顾 → 设置 → 会员升级主流程通过。
- 浏览器运行期无未处理错误；无 P0、P1、P2 遗留项。

final result: passed
